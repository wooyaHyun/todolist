{{>layout/header}}

<h2>로그인 화면 </h2>
<form class="form-signin" method="post" action="/login-proc">

    <input type="text" id="username" name="username" class="form-control" placeholder="아이디" autofocus="" />
    <input type="text" id="password" name="password" class="form-control" placeholder="비밀번호" />



<input id="joinBtn" type="submit" class="btn btn-secondary active" value="로그인" />
<a href="/user/join" class="btn btn-info active" role="button">회원가입</a>
</form>




{{>layout/footer}}

<script>

    const init = function(){
        const URLSearch = new URLSearchParams(location.search);
        console.log(URLSearch);
        const message = URLSearch.getAll("exception");

        if(message != ""){
            alert(message);
        }

    };

    init();
</script>